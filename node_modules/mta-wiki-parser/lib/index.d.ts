import { ISearchResults } from './search';
import { IParsedArticle } from './parse';
export declare class MTAWikiParser {
    static fetch(title: string, customUserAgent?: string): Promise<IParsedArticle>;
    static search(query: string, maxResults?: number, customUserAgent?: string): Promise<ISearchResults>;
}
