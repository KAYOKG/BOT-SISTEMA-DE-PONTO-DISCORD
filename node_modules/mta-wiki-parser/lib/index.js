"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fetch_1 = require("./fetch");
const search_1 = require("./search");
const parse_1 = require("./parse");
class MTAWikiParser {
    static async fetch(title, customUserAgent) {
        const fetchedArticle = await fetch_1.Fetcher.fetch(title, customUserAgent);
        const parsedArticle = parse_1.Parser.parse(fetchedArticle);
        return parsedArticle;
    }
    static async search(query, maxResults = 5, customUserAgent) {
        const results = await search_1.Searcher.search(query, maxResults, customUserAgent);
        return results;
    }
}
exports.MTAWikiParser = MTAWikiParser;
// import util from "util"
// async function test() {
//   const e = await MTAWikiParser.fetch("isElementWaitingForGroundToLoad")
//   console.log(util.inspect(e, false, null, true /* enable colors */))
// }
// test()
