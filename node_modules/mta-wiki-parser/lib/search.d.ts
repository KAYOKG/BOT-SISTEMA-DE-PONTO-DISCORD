export interface ISearchResults {
    query: string;
    hits: number;
    results: ISearchResult[];
}
export interface ISearchResult {
    title: string;
    pageid: number;
    size: number;
    wordcount: number;
    url: string;
}
export declare class Searcher {
    static search(query: string, maxResults: number, userAgent?: string): Promise<ISearchResults>;
}
